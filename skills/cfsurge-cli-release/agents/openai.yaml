interface:
  display_name: "Cfsurge CLI Release"
  short_description: "Use the released cfsurge zip CLI without cloning the repo"
  default_prompt: "Use $cfsurge-cli-release to guide a user through the released cfsurge CLI from GitHub Releases zip assets. Stay in standalone binary mode, ask for apiBase when needed, and give OS-specific commands for login, init, publish, list, remove, or --version."
